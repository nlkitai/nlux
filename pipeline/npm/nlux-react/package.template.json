{
  "name": "@nlux/nlux-react",
  "version": "{versions.nlux}",
  "description": "NLUX React is a library for building conversational AI interfaces, with support for OpenAI ChatGPT.",
  "repository": {
    "directory": "packages/react/nlux"
  },
  "dependencies": {
    "@nlux/nlux": "^{versions.nlux}",
    "react": "^{versions.dependencies.react}"
  }
}
