```typescript
import {createAiChat, ReadyCallback, ReadyEventDetails} from '@nlux/core';

const onReadyCallback: ReadyCallback = (readyEvent: ReadyEventDetails) => {
    console.log('Chat is ready. Props used to initialize the chat:', readyEvent.aiChatProps);
};
```
```typescript
// When creating the AiChat instance
const aiChat = createAiChat().withAdapter(adapter).on('ready', onReadyCallback);

// After creating the AiChat instance
aiChat.on('ready', onReadyCallback);
```
