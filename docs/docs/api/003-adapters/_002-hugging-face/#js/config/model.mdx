* Method: `withModel(model)`
* Argument Type: `string`
* Required: Either this property or an `Endpoint` must be provided
* Usage:

```js
const adapter = createChatAdapter()
    .withModel('The ID Of A Model Hosted On Hugging Face');
```
