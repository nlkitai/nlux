> * Method: `withConfig(config)`

> * Type: `LangServeConfig`

> * Usage:

```ts
const adapter = createChatAdapter()
    .withUrl('https://<My LangServe Runnable URL>')
    .withConfig({
        session_id: 'hi',
        requester_discord_id: 'mikeeeeey',
    });
```
