The `className` is an optional property.<br />
It can be used to provide a CSS class name to the `AiChat` component to apply custom styles.<br />
The new CSS class will be added to the root element of the `AiChat` component.

> * Type: `string`
> * Usage

```jsx
<AiChat className="my-class" />
```
