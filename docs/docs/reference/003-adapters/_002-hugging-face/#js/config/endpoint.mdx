> * Method: `withEndpoint(endpoint)`
> * Argument Type: `string`
> * Required: Either this property or a `Model` must be provided
> * Usage:

```js
const adapter = createChatAdapter()
    .withEndpoint(endpointUrl);
```
