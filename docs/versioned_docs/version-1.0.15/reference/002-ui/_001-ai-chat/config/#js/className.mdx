The `withClassName` method is used to provide a class name to the `AiChat` component.
The class name is optional, and it is used to add a class to the root element of the `AiChat` component.

* Type: `string`
* Optional
* Usage:

```javascript
const aiChat = createAiChat()
    .withClassName('my-class');
```
