> * Method: `withInputSchema(value)`
> * Argument Type: `boolean`
> * Default: `true`
> * Usage:

```js
const adapter = createChatAdapter()
    .withUrl('https://<My LangServe Runnable URL>')
    .withInputSchema(false);
```
