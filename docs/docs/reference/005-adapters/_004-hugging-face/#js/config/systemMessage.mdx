> * Method: `withSystemMessage(systemMessage)`
> * Argument Type: `string`
> * Usage:

```tsx
const adapter = createChatAdapter()
    .withSystemMessage('I want you to act as a customer support agent.');
```
