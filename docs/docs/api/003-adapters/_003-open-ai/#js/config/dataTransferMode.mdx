* Method: `withDataTransferMode(mode)`
* Argument Type: `'stream' | 'batch'`
* Required: `false`
* Default: `'stream'`
* Usage:

```js
const adapter = createUnsafeChatAdapter()
    .withDataTransferMode('stream');
```
