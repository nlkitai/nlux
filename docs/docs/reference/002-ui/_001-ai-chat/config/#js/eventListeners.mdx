In vanilla JavaScript, you can listen to `NLUX` events by registering a listener
using the `on(eventName, callback)` method on the `AiChat` instance.

You can visit [**the UI Events page**](/reference/ui/events) for detailed API documentation
on how to use event listeners.
