* Method: `withDataTransferMode(mode)`
* Argument Type: `'stream' | 'fetch'`
* Required: `false`
* Default: `'stream'`
* Usage:

```js
const adapter = createChatAdapter()
    .withDataTransferMode('stream');
```
